rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true
    }

// TODO: update firestore rules to make sense!
  //   match /users/{userId} {
  //     allow read: if isOwner(userId)
  //     allow write: if isOwner(userId) && isStudent(userId)
  //   }

  //   function isOwner(userId) {
  //     return request.auth.uid == userId
  //   }
  //   function isStudent(userId) {
  //     return incomingData(userId).email.mathes('.*[student].com')
  //   }
  //   function existingData() { return resource.data }
  //   function incomingData() { return request.resource.data }
  }
}