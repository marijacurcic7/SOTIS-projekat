rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // allow read if teacher is signed in and teacher is creator of the qti zip
      allow read: if request.auth != null && resource.metadata != null &&  resource.metadata.createdBy == request.auth.uid;
    }
  }
}